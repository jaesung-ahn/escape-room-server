# JWT Configuration
# IMPORTANT: Generate a secure random key for production
# You can generate a secure key using: openssl rand -base64 64
JWT_SECRET_KEY=your-secret-key-here-must-be-base64-encoded-min-32-bytes

# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/wiiee_db
DB_USERNAME=wiiee
DB_PASSWORD=wiiee_local_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# AWS Configuration
AWS_ACCESS_KEY=your-aws-access-key
AWS_SECRET_KEY=your-aws-secret-key
AWS_S3_BUCKET=your-s3-bucket-name

# Slack Configuration
SLACK_ENABLED=false
SLACK_TOKEN=your-slack-token
SLACK_CHANNEL=#your-channel

# Push Notification Configuration
PUSH_ENABLED=false
PUSH_API_URL=http://localhost:8082/api/push

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
